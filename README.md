# ls-rustified
super basic ls written in rust 

# how to install?
make sure rust and git are installed:

git clone https://github.com/Shadyshift/ls-rustified/
cd ls-rustified
cargo run  //this runs the project
OR
cargo build //this builds the project without running it
